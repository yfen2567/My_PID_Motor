# My_PID_Motor v2.1 构建记录 Manifest

> 本文件只是 v2.1 构建记录，不自动创建 tag，不自动 release，不代表固件已经发布。

## 1. 构建基线与检查状态

| 项目 | 内容 |
| --- | --- |
| 用户指定证据链 commit | `1b36b969ab0d50d009688248e2fac89f56d805e5` |
| 检查时实际 HEAD | `159b9c0e0cab9c2747fa0f9b4af203de11b620ca` |
| 检查时间 | 2026-06-29 11:40:58 +08:00 |
| 检查时 Git 工作区 | 干净 |
| 当前 manifest 更新后状态 | 仅本文件会产生未提交修改 |
| 本轮是否修改源码 / 工程配置 | 否 |
| 本轮是否 commit / push / tag / release | 否 |

注意：检查时实际 HEAD 不是用户指定的 `1b36b969ab0d50d009688248e2fac89f56d805e5`，而是 `159b9c0e0cab9c2747fa0f9b4af203de11b620ca`。两者之间的差异仅为 `reports/v2.1_build_manifest.md`，未发现 `.c`、`.h`、`.ioc`、链接脚本、启动文件、`.project`、`.cproject` 或 Makefile 差异。因此本次 Debug 构建的源码与工程配置基线可追溯到 `1b36b969ab0d50d009688248e2fac89f56d805e5`，但提交记录层面的实际 HEAD 需按 `159b9c0e0cab9c2747fa0f9b4af203de11b620ca` 记录。

## 2. 正式 Debug 构建记录

| 项目 | 内容 |
| --- | --- |
| 构建配置 | Debug |
| 构建方式 | STM32CubeIDE 手动 `Clean Project` + `Build Project` |
| 构建产物更新时间 | 2026-06-29 11:39:13 ～ 11:39:14 |
| 是否生成 `.elf` | 是 |
| 是否生成 `.map` | 是 |
| 是否生成 `.hex` | 否，当前 `Debug/` 下未发现 |
| 是否生成 `.bin` | 否，当前 `Debug/` 下未发现 |
| 构建是否成功 | 是，基于用户已完成 CubeIDE Build Project 且 ELF/MAP 在本次构建时间点更新 |
| 是否发现构建错误 | 未发现错误记录；未保存 CubeIDE Build Console 原始输出 |
| 是否发现构建警告 | 未确认；未保存 CubeIDE Build Console 原始输出，不能声称 0 warning |

## 3. 工程构建配置检查

| 项目 | 结果 |
| --- | --- |
| `.project` | 存在 |
| `.cproject` | 存在 |
| `My_PID_Motor.ioc` | 存在 |
| `.cproject` 中 Debug 配置 | 存在 |
| `.cproject` 中 Release 配置 | 存在 |
| `Debug/Makefile` | 存在 |
| `Release/Makefile` | 未发现 |

`.cproject` 中同时定义 Debug 和 Release 配置；但当前仓库只发现已生成的 `Debug/Makefile`，未发现 `Release/Makefile`。本次正式构建记录采用 Debug 配置。

## 4. 构建环境信息

| 工具 / 信息 | 检查结果 |
| --- | --- |
| STM32CubeIDE | 用户在 STM32CubeIDE 中手动执行 Clean + Build；命令行未读取到 CubeIDE 版本 |
| Debug Makefile 工具链标注 | `GNU Tools for STM32 (13.3.rel1)` |
| `mingw32-make` | `GNU Make 4.2.1`，路径：`F:\ToolChains\MinGW\bin\mingw32-make.exe` |
| 命令行 `arm-none-eabi-gcc` | 未在当前 shell PATH 和常见搜索路径中发现 |
| 命令行构建状态 | 上一轮命令行构建因缺少 `arm-none-eabi-gcc` 失败；本次正式产物来自 STM32CubeIDE 手动构建 |

## 5. 正式 Debug 构建产物

| 类型 | 路径 | 是否存在 | 大小 | 最后修改时间 | SHA-256 |
| --- | --- | --- | ---: | --- | --- |
| ELF | `Debug/My_PID_Motor.elf` | 是 | 1,209,744 bytes | 2026-06-29 11:39:13 | `59E57237C1A55A5F3668736A71362EBA53F84A24E883867DB58484EBF4E73709` |
| MAP | `Debug/My_PID_Motor.map` | 是 | 733,623 bytes | 2026-06-29 11:39:13 | `F32D808F969E931C147EB4119D5B52724F6AB25200E4867E9D6A5477C8DCEE0B` |
| HEX | `Debug/My_PID_Motor.hex` | 否 | — | — | — |
| BIN | `Debug/My_PID_Motor.bin` | 否 | — | — | — |

附加生成文件：

| 类型 | 路径 | 是否存在 | 大小 | 最后修改时间 | SHA-256 |
| --- | --- | --- | ---: | --- | --- |
| LIST | `Debug/My_PID_Motor.list` | 是 | 1,407,779 bytes | 2026-06-29 11:39:14 | `381A4BD78DC5AD926854DBEDB993F8F090C193092CBCA41F81D19850875A13E4` |

## 6. 产物更新判断

- `Debug/My_PID_Motor.elf` 的最后修改时间为 2026-06-29 11:39:13，晚于上一轮预检记录中的 2026-06-28 10:52:26，SHA-256 也已变化，因此判断为本次 CubeIDE `Clean Project` + `Build Project` 后更新的产物。
- `Debug/My_PID_Motor.map` 的最后修改时间为 2026-06-29 11:39:13，晚于上一轮预检记录中的 2026-06-28 10:52:26，SHA-256 也已变化，因此判断为本次 CubeIDE `Clean Project` + `Build Project` 后更新的产物。
- 当前未发现 `.hex` 或 `.bin`，说明 Debug 构建当前没有生成这两类输出文件；如 release checklist 要求 HEX/BIN，需要后续在 CubeIDE 输出转换设置中启用并重新构建记录。

## 7. 当前结论

本次 STM32CubeIDE 手动 Debug 构建已生成并更新 `.elf` 与 `.map`，未生成 `.hex` / `.bin`。当前没有保存 CubeIDE Build Console 原始输出，因此 manifest 不能声称“0 warning”，只能记录为未发现错误记录、警告状态未确认。

该 manifest 只记录构建事实和产物校验值，不自动 tag，不自动 release。

