# My_PID_Motor v2.1 构建记录 Manifest

> 本文件只是 v2.1 正式构建前检查与构建尝试记录，不自动创建 tag，不自动 release，不代表固件已经发布。

## 1. 基本信息

| 项目 | 内容 |
| --- | --- |
| 证据链 commit | `1b36b969ab0d50d009688248e2fac89f56d805e5` |
| 检查时间 | 2026-06-29 11:29:50 +08:00 |
| 当前 HEAD | `1b36b969ab0d50d009688248e2fac89f56d805e5` |
| 检查时工作区状态 | 干净 |
| 本轮是否修改源码 | 否 |
| 本轮是否 commit / push / tag / release | 否 |

## 2. 工程构建配置检查

| 项目 | 结果 |
| --- | --- |
| `.project` | 存在 |
| `.cproject` | 存在 |
| `My_PID_Motor.ioc` | 存在 |
| `.cproject` 中 Debug 配置 | 存在 |
| `.cproject` 中 Release 配置 | 存在 |
| `Debug/Makefile` | 存在 |
| `Release/Makefile` | 未发现 |

`.cproject` 中同时定义 Debug 和 Release 配置；但当前仓库只发现已生成的 `Debug/Makefile`，未发现 `Release/Makefile`。因此，若以当前仓库已有 Makefile 进行命令行构建，建议优先使用 Debug 配置。

若要使用 Release 作为正式候选产物，建议先在 STM32CubeIDE 中生成/执行 Release 配置，并重新记录产物、SHA-256 与构建输出。

## 3. 构建环境信息

| 工具 | 检查结果 |
| --- | --- |
| STM32CubeIDE 命令行入口 | 未在常见路径中发现 |
| `make` | 未在 PATH 中发现 |
| `mingw32-make` | `GNU Make 4.2.1`，路径：`F:\ToolChains\MinGW\bin\mingw32-make.exe` |
| `arm-none-eabi-gcc` | 未在 PATH 和常见搜索路径中发现 |
| Debug Makefile 工具链标注 | `GNU Tools for STM32 (13.3.rel1)` |

## 4. 命令行构建尝试

尝试命令：

```powershell
mingw32-make -C Debug all
```

结果：失败。

失败原因：`Debug/Makefile` 调用了 `arm-none-eabi-gcc`，但当前 shell 环境无法找到该程序。错误表现为 `CreateProcess(... arm-none-eabi-gcc ...) failed`，`make (e=2): 系统找不到指定的文件`，退出码为 `2`。

本次失败不是源码编译错误或链接错误；构建尚未进入有效编译阶段。构建尝试后检查到 Git 工作区仍然干净，已有 Debug 产物时间戳未更新。

## 5. 已发现构建产物

以下产物为本轮检查时仓库中已经存在的文件。由于命令行构建未成功，且这些文件生成时间早于当前证据链 commit，因此不能直接视为 v2.1 最终候选发布产物。

| 类型 | 路径 | 大小 | 文件时间 | SHA-256 | v2.1 候选状态 |
| --- | --- | ---: | --- | --- | --- |
| ELF | `Debug/My_PID_Motor.elf` | 1,209,744 bytes | 2026-06-28 10:52:26 | `9E41B556924882D9409EA2AF8AB564E2A4CC97313E13AA437DEFB1F7FEB26CB3` | 已盘点，但未确认为当前 commit 的正式构建产物 |
| MAP | `Debug/My_PID_Motor.map` | 733,663 bytes | 2026-06-28 10:52:26 | `71BAA316187A951C0503725C2E89CEE451F3F4EC4909EFCB34DF62D9BDED8B46` | 已盘点，但未确认为当前 commit 的正式构建产物 |
| ELF | `release/v2.0-rtos-cmdqueue/My_PID_Motor.elf` | 1,209,664 bytes | 2026-06-27 14:07:14 | `EF1E225921E258EC6686C19ED3FCB3988CFFCCF7FD27D9C4926037B06B6F288D` | v2.0 历史归档，不属于 v2.1 |

未发现可确认的 v2.1 `.hex` 或 `.bin` 产物。

## 6. 构建错误和警告

| 项目 | 结果 |
| --- | --- |
| 本轮命令行构建是否成功 | 否 |
| 是否发现源码编译错误 | 未进入有效编译阶段，无法判断 |
| 是否发现链接错误 | 未进入链接阶段，无法判断 |
| 是否发现编译警告 | 未进入有效编译阶段，无法判断 |
| 直接阻断原因 | 当前命令行环境缺少 `arm-none-eabi-gcc` |

## 7. 建议的 CubeIDE 手动构建步骤

如需生成正式 v2.1 候选构建产物，建议在 STM32CubeIDE 中执行：

1. 打开 STM32CubeIDE；
2. 导入或打开仓库：`F:\hal_stm32_project\My_PID_Motor`；
3. 确认当前 Git HEAD 为 `1b36b969ab0d50d009688248e2fac89f56d805e5`，并确认工作区干净；
4. 选择构建配置：
   - 若要与当前已有 Makefile 和此前调试记录保持一致，选择 `Debug`；
   - 若要以更接近发布产物的优化配置构建，选择 `Release`，但需先确认 CubeIDE 能生成 `Release/Makefile`，并重新记录 Release 产物；
5. 执行 `Project > Clean...`，选择当前工程和对应配置；
6. 执行 `Project > Build Project`；
7. 构建完成后记录 `.elf`、`.hex`、`.bin`、`.map` 的路径、大小、生成时间和 SHA-256；
8. 若生成 `.hex` / `.bin` 的输出转换未启用，请在 CubeIDE 的 MCU Post build / Output converter 设置中确认是否需要启用；不要为通过构建修改控制源码。

## 8. 当前结论

当前 HEAD 正确，工作区干净；工程具备 Debug 与 Release 配置定义，但命令行环境缺少 ARM GCC，导致本轮命令行构建未成功。现有 Debug ELF/MAP 已盘点并计算 SHA-256，但不能直接作为 `1b36b969ab0d50d009688248e2fac89f56d805e5` 的 v2.1 正式构建产物。

下一步应在 STM32CubeIDE 中执行手动构建，或将 `arm-none-eabi-gcc` 所在目录加入命令行 PATH 后重新构建，并更新本 manifest 中的正式产物记录。

