# My_PID_Motor v2.1-diagnostic-verification Release Notes（草稿）

> 本文件只是 release notes 草稿，不代表 v2.1 已正式发布，不创建 tag，也不构成工业级验证声明。

## 1. 版本定位

版本名称：`v2.1-diagnostic-verification`

本候选版本相对 `v2.0-rtos-cmdqueue` 的重点不是新增控制功能，而是补充诊断验证证据链：验证计划、日志字段字典、三档人工轻载扰动原始日志与报告、汇总报告、Release checklist、证据索引和边界说明。

当前 release notes 仍为草稿，不代表正式 release。v2.1 负载扰动证据链 commit 为 `1b36b969ab0d50d009688248e2fac89f56d805e5`；构建记录 manifest 后续记录到实际 HEAD `159b9c0e0cab9c2747fa0f9b4af203de11b620ca`，该差异仅涉及构建 manifest 文档，不涉及源码或工程配置。

## 2. 相对 v2.0 的变化

- 建立 `docs/verification/` 下的 v2.1 验证计划、日志字段字典、负载扰动计划和 Release checklist；
- 归档 LD-001/002/003 三组正式 LLCOM 原始日志；
- 形成三份单项报告和一份负载扰动汇总报告；
- 记录正式日志、报告、历史探索日志和 STM32CubeIDE 手动 Debug 构建产物的 SHA-256；
- 将旧手工运行、正反转、阶梯目标、PID 调参和早期负载日志与 v2.1 正式统计分离；
- README 已按 LD-001/002/003 完成事实更新验证状态，并保留不能外推边界。

以上变化以证据整理为主，不应表述为新增工业级控制能力。

## 3. 负载扰动汇总结论

测试条件为当前硬件、当前固件、UART 目标源、`kp=0.05, ki=0, kd=0` 和人工轻载扰动。

| 测试 | target | 单项结论 | 主要趋势 |
| --- | ---: | --- | --- |
| LD-001 | 500 | PASS（初步通过） | actual 均值下降 29.8，PWM 均值上升 1.4；释放确认后回到 baseline 附近 |
| LD-002 | 600 | PASS（轻载趋势初步通过，扰动幅度有限） | actual 均值下降 16.8，PWM 均值上升 0.9；释放确认后回到 baseline 附近 |
| LD-003 | 700 | PASS（轻载趋势初步通过，扰动幅度有限） | actual 均值下降 19.5，PWM 均值上升 0.9；释放确认后回到 baseline 附近 |

三组正式日志均记录到 `fault:NONE`；stop 后均为 `enable=0、State=IDLE、PWM=0`；`get fault` 均返回 `FAULT_SNAPSHOT_NONE`。按当前负载扰动计划，LD-001/002/003 负载扰动证据链已完成，可作为 v2.1 候选证据的一部分，不追加 LD-004。

该结论只表示 **人工轻载条件下观察到基本补偿趋势**，不是精确恢复时间、控制带宽、频响、转矩或工业级稳定性验证。

## 4. 已有证据支持的内容

- LD-001/002/003 均有正式原始日志、完整关键 MARK、释放确认段和单项报告；
- 三档施加载荷后 actual 均值下降，PWM 均值上升，变化方向符合软件补偿预期；
- 释放确认后 actual/PWM 回到各自 baseline 附近并继续运行；
- 三组状态输出均未记录 fault，stop 后软件状态与 PWM 命令归零；
- 三组无快照查询均返回可解释的 `FAULT_SNAPSHOT_NONE`；
- README、验证计划、日志字段字典、负载扰动计划、汇总报告和 checklist 均已存在。

## 5. 尚未完成或未验证

- Python 串口半自动回归尚未执行，可延期到后续阶段；
- 非法命令、越界命令和 run/stop 的候选固件可复现回归证据尚未按 v2.1 体系补齐；
- status 字段完整性尚未形成独立候选固件检查结论；
- 未自然出现 fault，因此真实 fault snapshot 有快照分支未验证；
- demo video 尚未录制，可延期；
- 已完成一次 STM32CubeIDE 手动 Debug Clean + Build，并记录 ELF/MAP/LIST 的 SHA-256；
- 当前未生成 HEX/BIN；是否需要 HEX/BIN 作为完整发布包的一部分仍待决定；
- 当前未保存 CubeIDE Build Console 原始输出，因此不能声称 0 warning；
- 完整 release tag、最终 release notes 和 commit—产物—日志唯一对应关系仍需项目负责人确认。
- PA8 物理 PWM 波形、方向脚、编码器波形、电流、真实转矩及长期运行没有在本轮验证。

## 6. 已知异常与保留记录

- **LD-001 第一次释放时序问题**：首次尝试的人工释放时机与 MARK 边界不可靠，已保存原始日志和归档报告，不纳入正式统计；
- **LD-001 stop 后单点 `actual=-85`**：PWM 已为 0，后续 actual 回到 0；异常点保留，现有日志不足以确定根因；
- **LD-003 启动瞬态 `actual=1200`**：出现在 `run=1` 后、baseline 前，后续回到正常范围；保留但不纳入 baseline、load 或 release 均值；
- 个别异步 status 在文件顺序上可能交织；正式统计依照 MARK 和固件 `ms` 分段，没有据此推断 MCU 复位。

这些异常没有被删除或简单归因为代码问题。

## 7. 不能外推的边界

本轮结果不能证明：

- 精确恢复时间、控制带宽、频响或严格动态性能；
- 长期可靠性、工业级稳定性或全部异常场景覆盖；
- 其他电机、驱动板、供电、负载或 PID 参数具有相同结果；
- 三次人工负载的大小和持续时间完全一致；
- 物理 PWM 波形、电流和真实转矩符合软件字段；
- 真实 fault snapshot 有快照分支可用。

## 8. 构建产物状态

已完成一次 STM32CubeIDE 手动 Debug `Clean Project` + `Build Project`，构建记录见 `reports/v2.1_build_manifest.md`。本记录是 Debug 构建记录，不是 Release 构建，也不表示 v2.1 已正式发布。

| 类型 | 路径 | 大小 | 最后修改时间 | SHA-256 | 状态 |
| --- | --- | ---: | --- | --- | --- |
| ELF | `Debug/My_PID_Motor.elf` | 1,209,744 bytes | 2026-06-29 11:39:13 | `59E57237C1A55A5F3668736A71362EBA53F84A24E883867DB58484EBF4E73709` | 已由 Debug Clean + Build 生成/更新 |
| MAP | `Debug/My_PID_Motor.map` | 733,623 bytes | 2026-06-29 11:39:13 | `F32D808F969E931C147EB4119D5B52724F6AB25200E4867E9D6A5477C8DCEE0B` | 已由 Debug Clean + Build 生成/更新 |
| LIST | `Debug/My_PID_Motor.list` | 1,407,779 bytes | 2026-06-29 11:39:14 | `381A4BD78DC5AD926854DBEDB993F8F090C193092CBCA41F81D19850875A13E4` | 附加生成文件，已记录 |
| HEX | `Debug/My_PID_Motor.hex` | — | — | — | 当前未生成 |
| BIN | `Debug/My_PID_Motor.bin` | — | — | — | 当前未生成 |

当前没有保存 CubeIDE Build Console 原始输出，因此只能记录为“未发现错误记录，警告状态未确认”，不能声称 0 warning。

## 9. 发布待办

- [x] README 的“已验证/待验证”状态已按 LD-001/002/003 完成事实复核；
- [ ] 确认 v2.1 候选范围和延期项；
- [ ] 确认是否需要 HEX/BIN 作为完整发布包的一部分；如需要，应启用输出转换并重新记录；
- [ ] 确认是否以 Debug 构建作为本轮候选构建，或另行执行 Release 构建；
- [ ] 提交 build manifest 与本 release notes 同步更新后的最终 commit hash；
- [ ] 建立最终 commit、Debug 构建产物和测试日志的对应关系；
- [ ] Python 串口半自动回归、demo video 等仍可延期，但不得写成已完成；
- [ ] 由项目负责人审阅 Release checklist；
- [ ] 后续如授权，再单独执行 tag / release；本草稿不执行这些操作。
